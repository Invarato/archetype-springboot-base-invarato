# For testing

# To disable for tests and pass fake parameters that the micro requires
spring.config.import=

# ----

# Auto Testcontainer (user: test, pass:test, bd: test)
spring.datasource.url=jdbc:tc:postgresql:18.0:///test
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=always

# Liquibase
spring.liquibase.enabled=false

spring.cache.type=none
